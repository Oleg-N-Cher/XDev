MODULE Delphi;

IMPORT SYSTEM;

PROCEDURE HiWord* (i: INT32): INT32;
BEGIN
  RETURN i DIV 10000H
END HiWord;

PROCEDURE LoWord* (i: INT32): INT32;
BEGIN
  RETURN ORD( BITS(i) * BITS(0FFFFH) )
END LoWord;

END Delphi.
